const hre = require("hardhat");

async function main() {
    // TODO: invoke the permitDestroy() function with valid inputs.
}

main().catch((error) => {
    console.error(error);
    process.exitCode = 1;
});
